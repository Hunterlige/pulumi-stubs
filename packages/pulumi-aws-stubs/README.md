# pulumi-aws-stubs

Type stubs for the `pulumi-aws` Python SDK.

This package provides `.pyi` files for better autocompletion and static type
checking in tools such as Pyright and Pylance.

## Install

```bash
pip install pulumi-aws-stubs
```
